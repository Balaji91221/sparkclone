services:
  spark:
    build: .
    ports: ["8000:8000"]
    env_file: .env
    volumes: ["spark_data:/srv/data"]
    environment:
      - DATABASE_URL=sqlite:////srv/data/spark.db
    restart: unless-stopped
volumes:
  spark_data:
